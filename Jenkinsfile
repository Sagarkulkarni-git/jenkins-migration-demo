pipeline {
  agent any

  environment {
    APP_ENV = 'dev'
  }

  stages {
    

    stage('Build') {
      steps {
        sh 'echo "Building app..."'
      }
    }

    stage('Deploy') {
      steps {
        sh 'echo "Deploying app..."'
      }
    }
  }
}
